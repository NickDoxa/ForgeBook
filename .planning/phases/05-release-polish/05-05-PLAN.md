---
phase: 05-release-polish
plan: 05
type: execute
wave: 3
depends_on: [02]
files_modified:
  - docs/COMPATIBILITY.md
autonomous: false
requirements: [REL-04]
tags: [docs, compatibility, human-verify]
must_haves:
  truths:
    - "docs/COMPATIBILITY.md exists with an 8-row compat-matrix table covering JEI, REI, Sodium/Embeddium, Iris/Oculus, Jade, Mouse Tweaks, Quark, Inventory HUD+ — the eight targets locked by REL-04."
    - "Matrix columns match RESEARCH §Compat Matrix Protocol: {Mod, Version tested, GUI scale 1 result, GUI scale 2 result, Notes}."
    - "Testing Protocol section documents the 9-step per-mod verification procedure (clean mods/ folder, drop ForgeBook + one compat target, launch, inspect button placement, test GUI scales 1 and 2, close, record)."
    - "Re-run triggers listed (ForgeBook UI changes, compat-mod major version bumps, user-reported regressions) so the matrix survives beyond Phase 5."
    - "Document scaffolds the matrix with '[ ] pending' placeholders for the 8 rows a human operator must fill in."
    - "The plan is marked `autonomous: false` because filling matrix entries requires visually running `./gradlew runClient` with each compat mod loaded, which Claude cannot do in a non-GUI harness — physical testing is deferred to the human operator via a human-verify checkpoint."
  artifacts:
    - path: "docs/COMPATIBILITY.md"
      provides: "Mod compatibility matrix + testing protocol + re-run triggers"
      contains: "Just Enough Items"
  key_links:
    - from: "README.md"
      to: "docs/COMPATIBILITY.md"
      via: "markdown link in Compatibility section (created by Plan 05-02 as a forward reference)"
      pattern: "\\[.*COMPATIBILITY.*\\]\\(docs/COMPATIBILITY\\.md\\)"
    - from: "docs/COMPATIBILITY.md Testing Protocol"
      to: "./gradlew runClient workflow"
      via: "manual verification steps referenced in Phase 4 UAT pattern"
      pattern: "gradlew runClient"
---

<objective>
Create `docs/COMPATIBILITY.md` — the public-facing mod-compatibility matrix for ForgeBook. The document consists of two parts: (1) a pre-filled skeleton matrix table listing the 8 REL-04-mandated compat-target mods with placeholder `[ ] pending` statuses and a known-good version string for each; (2) a "Testing Protocol" section prescribing the 9-step per-mod verification procedure any future contributor can follow to refresh the matrix. The plan is `autonomous: false` because actually filling the 8 rows requires physical `./gradlew runClient` runs with each compat mod loaded — deferred to a human-verify checkpoint.

Purpose: REL-04 commits ForgeBook to an ongoing compatibility posture across 8 popular mods. A blank matrix is useless; a frozen snapshot drifts. Shipping the protocol alongside the matrix makes the document self-refreshing — future Minecraft upgrades or compat-mod releases are addressed by running the documented protocol, not re-deriving it.

Output: One new file at `docs/COMPATIBILITY.md`. One human-verify checkpoint that formally parks the row-filling work. Under `--auto` chain the checkpoint auto-approves (physical testing deferred to a human operator; filled-in matrix arrives in a later session).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-release-polish/05-RESEARCH.md
@.planning/phases/05-release-polish/05-PATTERNS.md
@README.md

<interfaces>
<!-- Template from RESEARCH §Compat Matrix Protocol L528-603 -->
<!-- The 8 REL-04 compat targets with RESEARCH-verified version strings -->

The 8 targets (RESEARCH L559-567 for version strings):
- JEI (Just Enough Items) — jei-1.20.1-forge-15.20.0.106
- REI (Roughly Enough Items) — RoughlyEnoughItems-12.0.687
- Sodium (via Embeddium) — embeddium-0.3.30+mc1.20.1
- Iris (via Oculus) — oculus-mc1.20.1-1.7.0
- Jade — Jade-1.20.1-forge-11.12.1
- Mouse Tweaks — MouseTweaks-forge-mc1.20-2.25
- Quark — Quark-4.0-460
- Inventory HUD+ — InventoryHUD.Forge-1.20.1-3.4.25

These version strings are what the Phase 5 planner expects operators to test against. Operators may substitute newer versions — the matrix is self-updating via the "Re-run triggers" section.

Testing protocol — 9 steps per mod, RESEARCH L572-585.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/COMPATIBILITY.md with skeleton matrix + testing protocol</name>
  <files>docs/COMPATIBILITY.md</files>
  <read_first>
    - .planning/phases/05-release-polish/05-RESEARCH.md §"Compat Matrix Protocol" (L528-603) — full template
    - .planning/phases/05-release-polish/05-PATTERNS.md §"docs/COMPATIBILITY.md" (L494-501)
    - README.md (verify the forward-reference link target matches the filename created here)
  </read_first>
  <action>
    Create `docs/` directory if it does not exist:
    ```bash
    mkdir -p docs
    ```

    Create `docs/COMPATIBILITY.md` with this exact structure (template from RESEARCH L548-591):

    ````markdown
    # ForgeBook Compatibility Matrix

    **Last verified:** YYYY-MM-DD on Minecraft 1.20.1 + Forge 47.4.18
    **Tester:** (fill in when row is verified)
    **Method:** see §Testing Protocol below

    ForgeBook is a client+server mod; incompatibility most commonly shows up as overlapping GUI widgets in the inventory screen, where ForgeBook injects a toggle button at the right edge of the inventory frame. The mods below are community-popular and commonly installed alongside ForgeBook; this matrix records whether the "Ask ForgeBook" button and ChatScreen render cleanly in each combination.

    ## Matrix

    | Mod | Version tested | GUI scale 1 | GUI scale 2 | Notes |
    |-----|----------------|-------------|-------------|-------|
    | Just Enough Items (JEI) | jei-1.20.1-forge-15.20.0.106 | [ ] pending | [ ] pending | — |
    | Roughly Enough Items (REI) | RoughlyEnoughItems-12.0.687 | [ ] pending | [ ] pending | — |
    | Sodium (Embeddium fork) | embeddium-0.3.30+mc1.20.1 | [ ] pending | [ ] pending | — |
    | Iris (Oculus fork) | oculus-mc1.20.1-1.7.0 | [ ] pending | [ ] pending | — |
    | Jade | Jade-1.20.1-forge-11.12.1 | [ ] pending | [ ] pending | — |
    | Mouse Tweaks | MouseTweaks-forge-mc1.20-2.25 | [ ] pending | [ ] pending | — |
    | Quark | Quark-4.0-460 | [ ] pending | [ ] pending | — |
    | Inventory HUD+ | InventoryHUD.Forge-1.20.1-3.4.25 | [ ] pending | [ ] pending | — |

    Legend: `✓` pass · `✗` fail (see Notes) · `[ ] pending` not yet tested

    ## Testing Protocol

    For each mod in the matrix:

    1. Fresh `./gradlew runClient` with a clean `run/mods/` folder (delete any stale jars first).
    2. Drop the ForgeBook jar (`build/libs/forgebook-1.0.0.jar`) **and** the one compat target into `run/mods/`.
    3. Launch.
    4. Open the inventory — verify the "Ask ForgeBook" button appears to the right of the inventory at the `leftPos+imageWidth+4` offset.
    5. Verify no vanilla or compat-mod widgets overlap the button.
    6. Click the button — verify the ChatScreen opens with the inventory still visible beneath.
    7. Change GUI scale via Options → Video Settings → GUI Scale to 1, repeat steps 4-6. Then to 2, repeat.
    8. Close Minecraft.
    9. Record result in the matrix above. Any overlap or failure becomes a "note" entry linking to a GitHub issue.

    ## Re-run Triggers

    Re-run this protocol when:

    - ForgeBook ships a UI-affecting change (new widget, panel tier, inventory-injected surface).
    - Any compat target releases a major version bump.
    - Users report a compatibility regression via GitHub issues.

    ## Contributing Matrix Updates

    1. Run the protocol above for the affected mod(s).
    2. Update the matrix row with the tested version string and result (`✓` or `✗`).
    3. If failing, add a Notes entry describing the symptom and linking to a GitHub issue.
    4. Update the `**Last verified:**` date header to the current date.
    5. Submit a PR with the updated `docs/COMPATIBILITY.md`.
    ````

    **Do NOT:**
    - Fill in any row with `✓` — every row starts as `[ ] pending`. Only a human operator running `./gradlew runClient` with each compat mod loaded can verify and mark them pass/fail.
    - Invent a testing harness or Gradle task that spins up 8 client-with-mod configs. RESEARCH §"Mod-compat testing harness" (L212-213) explicitly rejects automation: each compat mod needs a different config, some conflict, automation cost > value.
    - Reference a `docs/` path that does not exist in the repo — this is the FIRST file in `docs/`; create the directory.
    - Substitute different compat targets than the 8 REL-04 specifies. The list is locked.
  </action>
  <verify>
    <automated>test -d docs &amp;&amp; test -f docs/COMPATIBILITY.md &amp;&amp; grep -q "Just Enough Items (JEI)" docs/COMPATIBILITY.md &amp;&amp; grep -q "Roughly Enough Items (REI)" docs/COMPATIBILITY.md &amp;&amp; grep -q "Embeddium" docs/COMPATIBILITY.md &amp;&amp; grep -q "Oculus" docs/COMPATIBILITY.md &amp;&amp; grep -q "Jade" docs/COMPATIBILITY.md &amp;&amp; grep -q "Mouse Tweaks" docs/COMPATIBILITY.md &amp;&amp; grep -q "Quark" docs/COMPATIBILITY.md &amp;&amp; grep -q "Inventory HUD+" docs/COMPATIBILITY.md &amp;&amp; grep -q "GUI scale 1" docs/COMPATIBILITY.md &amp;&amp; grep -q "GUI scale 2" docs/COMPATIBILITY.md &amp;&amp; grep -q "Testing Protocol" docs/COMPATIBILITY.md &amp;&amp; grep -q "Re-run Triggers" docs/COMPATIBILITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/COMPATIBILITY.md` exists.
    - Matrix has at least 8 data rows (one per compat target): `grep -cE '^\|\s*(Just Enough|Roughly Enough|Sodium|Iris|Jade|Mouse Tweaks|Quark|Inventory HUD)' docs/COMPATIBILITY.md` returns 8.
    - All 8 compat mod names present (grep each name individually):
      - `grep -c "Just Enough Items (JEI)" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "Roughly Enough Items (REI)" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "Embeddium" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "Oculus" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "Jade" docs/COMPATIBILITY.md` returns ≥ 1.
      - `grep -c "Mouse Tweaks" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "Quark" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "Inventory HUD+" docs/COMPATIBILITY.md` returns 1.
    - All 8 rows have `[ ] pending` placeholders for BOTH GUI scale columns: `grep -c "\[ \] pending" docs/COMPATIBILITY.md` returns ≥ 16 (2 per row × 8 rows).
    - Version strings present (8 known-good versions):
      - `grep -c "jei-1.20.1-forge-15.20.0.106" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "RoughlyEnoughItems-12.0.687" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "embeddium-0.3.30" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "oculus-mc1.20.1-1.7.0" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "Jade-1.20.1-forge-11.12.1" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "MouseTweaks-forge-mc1.20-2.25" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "Quark-4.0-460" docs/COMPATIBILITY.md` returns 1.
      - `grep -c "InventoryHUD.Forge-1.20.1-3.4.25" docs/COMPATIBILITY.md` returns 1.
    - Both GUI scales mentioned: `grep -c "GUI scale 1" docs/COMPATIBILITY.md` returns ≥ 1; `grep -c "GUI scale 2" docs/COMPATIBILITY.md` returns ≥ 1.
    - Testing Protocol section exists: `grep -c "## Testing Protocol" docs/COMPATIBILITY.md` returns 1.
    - Re-run Triggers section exists: `grep -c "## Re-run Triggers" docs/COMPATIBILITY.md` returns 1.
    - Forward-reference link from README resolves (README Plan 05-02 links to `docs/COMPATIBILITY.md` which now exists): `test -f README.md && grep -q "docs/COMPATIBILITY.md" README.md`.
  </acceptance_criteria>
  <done>
    `docs/COMPATIBILITY.md` is checked in with the 8-row skeleton matrix, 9-step Testing Protocol, Re-run Triggers, and Contributing instructions. All 8 rows start as `[ ] pending` awaiting human verification. The forward-reference link from README.md (plan 05-02) now resolves to a real file.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human operator fills in at least one matrix row (JEI recommended)</name>
  <files>docs/COMPATIBILITY.md</files>
  <action>See &lt;how-to-verify&gt; below for step-by-step operator instructions. This is a human-verify checkpoint — Claude cannot run ./gradlew runClient with compat mods loaded in a non-GUI harness. Under --auto chain the checkpoint auto-approves (physical row-filling deferred to a post-release operator session).</action>
  <verify><manual>Operator reports approved (state accepted as-is), verified (ran protocol for at least one row), or deferred (parked for later session).</manual></verify>
  <done>Operator has read the Testing Protocol in docs/COMPATIBILITY.md and either (a) filled at least one row or (b) explicitly acknowledged deferral for a post-release session. Under --auto chain this resolves automatically.</done>
  <what-built>
    Plan 05-05 Task 1 created `docs/COMPATIBILITY.md` with a skeleton matrix (8 compat targets × 2 GUI scales = 16 cells, all `[ ] pending`) and the 9-step Testing Protocol prose.
  </what-built>
  <how-to-verify>
    This checkpoint formalizes what a human operator must do to close REL-04 SC-1 ("manually verified at GUI scales 1 and 2"). Filling all 8 rows requires 8 separate `./gradlew runClient` launches, each with a different compat mod dropped into `run/mods/` — approximately 15-30 min per mod, 2-4 hours total.

    **Minimum to satisfy REL-04 in this phase:** fill one row (JEI recommended — highest-impact compat target, similar overlay math to ForgeBook's button).

    **Step-by-step for JEI:**
    1. Download `jei-1.20.1-forge-15.20.0.106.jar` from CurseForge or the jeimei builds page.
    2. Build the ForgeBook jar: `./gradlew clean build --no-daemon`. Confirm `build/libs/forgebook-1.0.0.jar` exists.
    3. Prepare a clean `run/mods/` folder:
       ```bash
       rm -f run/mods/*.jar 2>/dev/null || true
       cp build/libs/forgebook-1.0.0.jar run/mods/
       cp /path/to/jei-1.20.1-forge-15.20.0.106.jar run/mods/
       ```
    4. Launch: `./gradlew runClient --no-daemon`.
    5. In-game: open the inventory (`E`). Verify the "Ask ForgeBook" button appears to the right of the inventory at the expected offset. Verify JEI's sidebar does NOT overlap the button.
    6. Click the button — verify `ChatScreen` opens; inventory still visible beneath.
    7. Options → Video Settings → GUI Scale → 1 (small). Re-open inventory. Repeat verification.
    8. GUI Scale → 2. Repeat.
    9. Close the client.
    10. Edit `docs/COMPATIBILITY.md`: change JEI's `[ ] pending` to `✓` in both GUI scale columns, add a brief Notes entry like "ForgeBook button placed right of inventory; JEI overlay to the right. No overlap."
    11. Commit: `docs(phase-05): fill JEI row in compatibility matrix`.

    **Under `--auto` chain:** This checkpoint auto-approves (physical testing deferred). The unfilled matrix is acceptable for phase close; REL-04 SC is satisfied by the protocol + skeleton being shipped, and the human operator fills rows post-release as time permits. This is explicitly allowed per RESEARCH §"What gets shipped in Phase 5" (L593-599).

    **Pass criterion (full verification):** At least one row marked `✓` in both GUI scale columns with a Notes entry describing observed button placement.

    **Pass criterion (deferred verification):** Acceptance = confirmation that the operator has read the Testing Protocol and will run it when time permits. Matrix remains all-pending for now.
  </how-to-verify>
  <resume-signal>Type "approved" to accept current state (skeleton + protocol + at most partial rows filled), or "deferred" to explicitly park physical testing for a post-release session.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/COMPATIBILITY.md → server operator's install decision | Outdated "known-good" version strings could mislead a new operator into pinning the wrong compat-mod version, causing silent breakage on Minecraft upgrade. |
| Skeleton matrix (all [ ] pending) → user trust | A matrix that never moves past [ ] pending suggests ForgeBook is abandoned. Re-run Triggers section gives operators the tools to update it themselves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information Disclosure | Stale version strings in matrix | mitigate | Version strings come from RESEARCH L559-567 (researcher-verified 2026-04-16). "Re-run Triggers" section instructs operators to update versions when compat mods bump — document is self-refreshing. |
| T-05-05-02 | Tampering | Someone fills row with `✓` without running the protocol | accept | Trust model: matrix contributors run the protocol. PR review catches egregious cases. Protocol is documented; integrity rests on the author. |
| T-05-05-03 | Denial of Service | Broken forward-reference from README.md | mitigate | Plan 05-02 creates the README with link target `docs/COMPATIBILITY.md`; Plan 05-05 creates that file. Both land in the same phase. Any operator viewing the repo mid-Phase-5 sees working links once Wave 3 completes. |
| T-05-05-04 | Repudiation | Matrix filled with unverified claim | accept | `**Tester:** (fill in when row is verified)` field tracks attribution. Protocol documents the `**Last verified:**` field convention — if a row's date is suspicious the reader can re-run the protocol themselves. |
| T-05-05-05 | Elevation of Privilege | Test instructions suggesting OP-level actions | accept | Matrix testing exercises ForgeBook's client-side UI only — no server-side OP commands needed. No privilege-escalation risk in the protocol itself. |
| T-05-05-06 | Spoofing | Wrong mod names/versions in matrix tricking user into downloading malicious lookalikes | mitigate | Official full names used (e.g., "Just Enough Items (JEI)", "Roughly Enough Items (REI)"); the abbreviations match their authors' canonical naming. Operators download from the official CurseForge/Modrinth pages (Testing Protocol step 1 implies this — reinforced by "known-good version" language). |

</threat_model>

<verification>

1. **File + directory existence:** `test -d docs && test -f docs/COMPATIBILITY.md`.
2. **8 compat targets named:** the per-name greps in the task acceptance criteria all return 1.
3. **16 pending cells:** `grep -c "\[ \] pending" docs/COMPATIBILITY.md` returns ≥ 16.
4. **Testing Protocol + Re-run Triggers + Contributing sections present.**
5. **README forward reference resolves:** `test -f README.md && grep -q "docs/COMPATIBILITY.md" README.md`.
6. **Human-verify checkpoint posted (plan autonomous=false wires into gsd-execute-phase checkpoint-pause flow; under --auto chain auto-approves).**

</verification>

<success_criteria>

- [ ] `docs/COMPATIBILITY.md` exists with the 8-row skeleton matrix.
- [ ] Each compat mod named with its known-good version string sourced from RESEARCH.
- [ ] Testing Protocol describes the 9 steps per mod.
- [ ] Re-run Triggers section lists the three refresh triggers (UI change, compat bump, user report).
- [ ] Contributing instructions tell future PR authors how to update rows.
- [ ] README.md forward-reference (Plan 05-02) now resolves.
- [ ] Human-verify checkpoint posted; under --auto chain it auto-approves and Physical row-filling is parked for a post-release operator session.

</success_criteria>

<output>
After completion, create `.planning/phases/05-release-polish/05-05-SUMMARY.md` recording: file created, matrix row count (8), pending-cell count (16), and checkpoint resolution ("approved" or "deferred").
</output>
