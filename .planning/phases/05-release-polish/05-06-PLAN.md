---
phase: 05-release-polish
plan: 06
type: execute
wave: 3
depends_on: ["01", "02", "04"]
files_modified:
  - docs/RELEASE-SMOKE.md
autonomous: false
requirements: [REL-05]
tags: [docs, release, smoke, human-verify]
must_haves:
  truths:
    - "docs/RELEASE-SMOKE.md exists with a 9-step protocol covering clean build, dedicated-server install, secret handling, client connect, smoke /forgebook item, smoke chat UI, smoke disable/enable, smoke stats/reload, teardown."
    - "Step 1 (clean build) is AUTOMATED — `./gradlew clean build --no-daemon` + `jar tf` checks jsoup relocation. Claude can run this step."
    - "Steps 2-9 are HUMAN-ONLY — require a disposable Minecraft 1.20.1 installation, disposable Forge 47.4.18 dedicated server, and a real Anthropic API key. Claude cannot run these in a non-GUI harness."
    - "Pass/Fail criteria defined: PASS if every 'Expected' line matches observed behaviour AND no stack traces or secret leakage in logs/latest.log; FAIL = tag as -rcN, file issues, fix, re-run."
    - "Plan is `autonomous: false` because the human-only steps require physical machine access, network connectivity, and a paid API key. Under --auto chain this checkpoint auto-approves (physical release smoke deferred to a pre-tag human session)."
    - "Jar filename in the protocol is `forgebook-1.0.0.jar` — reflects Plan 05-01's version bump. Protocol is not valid before Plan 05-01 ships (depends_on: [01])."
  artifacts:
    - path: "docs/RELEASE-SMOKE.md"
      provides: "9-step release smoke protocol with bash snippets for the automated parts"
      contains: "forgebook-1.0.0.jar"
  key_links:
    - from: "docs/RELEASE-SMOKE.md Step 1"
      to: "build/libs/forgebook-1.0.0.jar"
      via: "./gradlew clean build + jar tf verification"
      pattern: "gradlew clean build"
    - from: "docs/RELEASE-SMOKE.md Step 3"
      to: "config/forgebook-server.toml"
      via: "chmod 600 file-permission restriction cross-referenced from README security posture"
      pattern: "chmod 600"
---

<objective>
Create `docs/RELEASE-SMOKE.md` — the pre-tag release smoke protocol that a human operator walks before applying a `v1.0.0` (or later) git tag. The document is a 9-step checklist: Step 1 is automated (clean build + jar-integrity checks Claude can run); Steps 2-9 are human-only (disposable dedicated server install, API-key configuration, in-game verification of `/forgebook item`, chat UI, disable/enable/stats/reload). The plan is `autonomous: false` — under `--auto` chain the human-verify checkpoint auto-approves, physical smoke deferred to a pre-release human session.

Purpose: REL-05 guards against the "dev run works but packaged jar doesn't" failure mode. `./gradlew runClient` / `runServer` both run from the exploded classpath, NOT the built jar. The relocated jsoup jar-in-jar could silently break at package time without any dev-run signal. This protocol exists specifically to catch that — by running the `forgebook-1.0.0.jar` as a drop-in on a clean Forge 47.4.18 dedicated server, the jarJar + relocation + SimpleChannel wiring are all exercised as a real user would see them.

Output: One new file at `docs/RELEASE-SMOKE.md`. One human-verify checkpoint that formally parks the physical smoke for the release operator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-release-polish/05-RESEARCH.md
@.planning/phases/05-release-polish/05-PATTERNS.md
@README.md
@build.gradle

<interfaces>
<!-- Template from RESEARCH §Prod-Jar Smoke Protocol L606-720 -->
<!-- Key facts the executor needs -->

Jar filename after Plan 05-01's version bump:
- Before: `build/libs/forgebook-0.1.0.jar`
- After:  `build/libs/forgebook-1.0.0.jar`   ← what this protocol targets

Jar contents assertion (Step 1): `jar tf build/libs/forgebook-1.0.0.jar | grep -E "META-INF/jarjar/.*jsoup.*\.jar"` must match — confirms jsoup relocation + jarJar nesting survived the production build.

Log lines to grep for after dedicated-server startup:
- `Loading mod 'forgebook' (version 1.0.0)` — confirms version bump landed.
- `ForgebookNetwork registered channel` — SimpleChannel wiring up.
- `SystemPromptBuilder.buildAndCache completed` — AI pipeline initialized.
- `RateLimiterHolder.swap seeded` — safety primitives initialized.

Secret-leakage grep:
- After setting `ai_api_key = "sk-ant-..."` and running a request, `grep "sk-ant-" logs/latest.log` must return 0 lines — the ApiKeyScrubFilter (Phase 1 CFG-05) is working.

Smoke commands (on a connected, OP'd client):
- `/give <yourname> minecraft:diamond_pickaxe` — gives a known item.
- `/forgebook item` — holds pickaxe, expects AI reply + `Source: <url>` citation.
- `/forgebook ask <question>` — one-shot chat reply.
- `/forgebook disable` / `/forgebook enable` — toggles kill switch.
- `/forgebook stats` — shows per-player counters.
- `/forgebook reload` — atomic config reload after toml edit.

Audit log line expected format (Phase 3 SAFE-04):
`[forgebook.audit] uuid=<uuid> kind=ITEM tokens=<n> latency_ms=<n> outcome=SUCCESS`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/RELEASE-SMOKE.md with 9-step protocol</name>
  <files>docs/RELEASE-SMOKE.md</files>
  <read_first>
    - .planning/phases/05-release-polish/05-RESEARCH.md §"Prod-Jar Smoke Protocol" (L606-720) — full template
    - .planning/phases/05-release-polish/05-PATTERNS.md §"docs/RELEASE-SMOKE.md" (L504-508)
    - build.gradle (confirms jar name pattern and `jarJar` task chain)
  </read_first>
  <action>
    Ensure `docs/` directory exists (Plan 05-05 Task 1 creates it if run first; this task guards against ordering):
    ```bash
    mkdir -p docs
    ```

    Create `docs/RELEASE-SMOKE.md` with this exact structure (template from RESEARCH L614-713):

    ````markdown
    # ForgeBook Release Smoke Protocol

    **Before tagging a release, walk this protocol from a clean working tree.**

    This protocol catches issues invisible to the dev loop: `./gradlew runClient` / `runServer` run from the exploded classpath, NOT the built jar. The bundled jsoup (relocated + jarJar'd) and the SimpleChannel wiring only get exercised in their final form when the built jar is loaded by a stock Forge installation.

    ## Prerequisites

    - A disposable Minecraft 1.20.1 installation (NOT your main launcher).
    - A disposable Forge 1.20.1-47.4.18 dedicated server installed to a fresh folder.
    - An Anthropic API key (test key or main key — rate limits apply; a single `/forgebook item` costs ≤ $0.01 at current Haiku pricing).

    ## Step 1 — Clean build (AUTOMATED — Claude can run this)

    ```bash
    ./gradlew clean build --no-daemon
    ls build/libs/forgebook-1.0.0.jar     # must exist and be >50 KB
    jar tf build/libs/forgebook-1.0.0.jar | grep jsoup   # must show nested META-INF/jarjar/*.jar with jsoup
    ```

    **Expected:**
    - `build/libs/forgebook-1.0.0.jar` exists.
    - `jar tf` output includes a line matching `META-INF/jarjar/jsoup-relocated-*.jar`.
    - Clean build exits 0.

    ## Step 2 — Dedicated server install (HUMAN)

    1. Fresh Forge 1.20.1-47.4.18 dedicated server folder. Start once with no mods to generate the EULA; accept it (edit `eula.txt`, set `eula=true`).
    2. Stop the server.
    3. Copy `build/libs/forgebook-1.0.0.jar` into the server's `mods/` folder.
    4. Start the server.

    **Expected in console / `logs/latest.log`:**
    - `Loading mod 'forgebook' (version 1.0.0)` appears.
    - No `NoClassDefFoundError` referencing `com.forgebook.shadow.jsoup.*` classes.
    - No client-classloader leakage — nothing about `net.minecraft.client.*` being loaded (firewall is holding).
    - Log lines confirming: `ForgebookNetwork` channel `forgebook:main` registered, `ConfigHolder` loaded a snapshot, `SystemPromptBuilder.buildAndCache` completed, `RateLimiterHolder.swap` seeded.

    ## Step 3 — Set secrets (HUMAN)

    1. Stop the server.
    2. Edit `config/forgebook-server.toml`:
       ```toml
       ai_api_key = "sk-ant-..."
       ```
    3. Restrict file permissions:
       ```bash
       chmod 600 config/forgebook-server.toml
       ```
       On Windows, set the NTFS ACL so only the server-running account can read.
    4. Restart the server.
    5. After startup, verify secret leakage is scrubbed:
       ```bash
       grep "sk-ant-" logs/latest.log
       ```
       **Expected:** zero matching lines. The `ApiKeyScrubFilter` (Phase 1 CFG-05) redacts all `sk-ant-` prefixed substrings from logs.

    ## Step 4 — Client connect (HUMAN)

    1. In your disposable Minecraft 1.20.1 launcher, install Forge 47.4.18.
    2. Copy the SAME `forgebook-1.0.0.jar` into the client's `mods/` folder.
    3. Launch the client; connect to the dedicated server (`localhost` or the server's IP).
    4. Op your player from the server console: `op <yourname>`.

    **Expected:** client joins cleanly; no "mod mismatch" disconnect; chat shows no red error messages.

    ## Step 5 — Smoke `/forgebook item` (HUMAN)

    1. In-game, give yourself a diamond pickaxe: `/give <yourname> minecraft:diamond_pickaxe`.
    2. Hold the pickaxe.
    3. Run `/forgebook item`.

    **Expected:**
    - An AI reply appears in chat within 5-15 seconds explaining the item.
    - A `Source: <url>` citation appears at the bottom of the reply (CMD-07).
    - On the server: `logs/latest.log` contains exactly ONE `[forgebook.audit]` line with the shape:
      `uuid=<your-uuid> kind=ITEM tokens=<positive> latency_ms=<positive> outcome=SUCCESS`.

    ## Step 6 — Smoke the chat UI (HUMAN)

    1. Open inventory (`E`). Verify the "Ask ForgeBook" button appears to the right of the inventory.
    2. Click the button. Verify `ChatScreen` opens next to (not replacing) the inventory.
    3. Type "What is a diamond pickaxe?" and press Enter.
    4. Verify a loading indicator appears, followed by an assistant reply bubble.
    5. Close the screen (ESC). Re-open the inventory, click the button — verify the conversation is empty (UI-05 session clear).

    ## Step 7 — Smoke `/forgebook disable` + `/forgebook enable` (HUMAN)

    1. `/forgebook disable` → expect `ForgeBook disabled. New requests will return DISABLED.` (via i18n key `forgebook.command.disable.success` post-Plan 05-04).
    2. `/forgebook item` → expect `ForgeBook is temporarily disabled by an operator.` (via `forgebook.command.denied.disabled`).
    3. `/forgebook enable` → expect `ForgeBook enabled. New requests will be processed.`.
    4. `/forgebook item` → expect normal AI reply (kill switch released).

    ## Step 8 — Smoke `/forgebook stats` and `/forgebook reload` (HUMAN)

    1. `/forgebook stats` → expect a table with at least your request counted (and estimated tokens / latency).
    2. Edit `config/forgebook-server.toml` — change `rate_limit_per_minute` to `1`.
    3. `/forgebook reload` → expect `ForgeBook config + system prompt reloaded.` (via `forgebook.command.reload.success`).
    4. As a non-OP player (de-op yourself or ask a second account): run `/forgebook ask hi` twice in quick succession.
       **Expected:** the second call is RATE_LIMITED with a retry-after message — `Rate limit reached. Try again in Ns.` (via `forgebook.command.denied.rate_limited`).

    ## Step 9 — Teardown (HUMAN)

    - Stop the client and server.
    - **Delete the API key from `config/forgebook-server.toml` before archiving**.
    - Delete the disposable server folder and client instance.

    ## Pass / Fail Criteria

    **PASS** if every "Expected" line matches observed behaviour AND no stack traces AND no secret leakage (Step 3 grep returns zero).

    **FAIL** = tag the release candidate as `v1.0.0-rc<N>` (not `v1.0.0`); file GitHub issues for each failure; fix; re-run this protocol from Step 1.

    ## What's automated vs. human

    | Step | Automated? | Why |
    |------|-----------|-----|
    | 1 — Clean build + jar-integrity | Yes | Pure Gradle + jar tf; no game needed. Claude can run this. |
    | 2-9 — Everything else | No | Needs a disposable MC launcher, disposable Forge server, real Anthropic API key, visual in-game verification. |

    Under GSD `--auto` chain, steps 2-9 are deferred to a human operator; the physical smoke happens pre-tag (can be minutes before `git tag v1.0.0`).
    ````

    **Do NOT:**
    - Invent a CI job that runs `/forgebook item` against live Claude — requires a secret in CI (cost exposure) and a live internet connection. Phase 1's GameTest harness already covers the mod-loading side in hermetic CI; REL-05 is specifically about the PACKAGED-jar + live-API smoke.
    - Reference the old `forgebook-0.1.0.jar` filename — Plan 05-01 bumps to `1.0.0`; this protocol depends on that and the jar filename must match.
    - Skip the `chmod 600` step — REL-03 and REL-05 both touch file permissions; the protocol must reinforce the README guidance.
    - Suggest a "lighter" smoke that only tests the server half — Step 4 (client connect) is specifically the UI-05 / UI-06 regression guard per Phase 4 UAT.
  </action>
  <verify>
    <automated>test -d docs &amp;&amp; test -f docs/RELEASE-SMOKE.md &amp;&amp; grep -q "forgebook-1.0.0.jar" docs/RELEASE-SMOKE.md &amp;&amp; grep -q "./gradlew clean build" docs/RELEASE-SMOKE.md &amp;&amp; grep -q "chmod 600" docs/RELEASE-SMOKE.md &amp;&amp; grep -q "jar tf" docs/RELEASE-SMOKE.md &amp;&amp; grep -q "/forgebook item" docs/RELEASE-SMOKE.md &amp;&amp; grep -q "/forgebook reload" docs/RELEASE-SMOKE.md &amp;&amp; grep -q "Step 1" docs/RELEASE-SMOKE.md &amp;&amp; grep -q "Step 9" docs/RELEASE-SMOKE.md &amp;&amp; grep -q "Pass / Fail" docs/RELEASE-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/RELEASE-SMOKE.md` exists.
    - All 9 steps labelled: `grep -cE "^## Step [1-9] " docs/RELEASE-SMOKE.md` returns 9.
    - Jar filename correct: `grep -c "forgebook-1.0.0.jar" docs/RELEASE-SMOKE.md` returns ≥ 2 (Step 1 build output + Step 2 server install).
    - Clean-build command: `grep -c "./gradlew clean build --no-daemon" docs/RELEASE-SMOKE.md` returns ≥ 1.
    - jar-tf command: `grep -c "jar tf build/libs/forgebook-1.0.0.jar" docs/RELEASE-SMOKE.md` returns ≥ 1.
    - chmod 600 cross-reference: `grep -c "chmod 600" docs/RELEASE-SMOKE.md` returns ≥ 1.
    - All 7 `/forgebook` subcommands exercised:
      - `grep -c "/forgebook item" docs/RELEASE-SMOKE.md` returns ≥ 1.
      - `grep -c "/forgebook ask" docs/RELEASE-SMOKE.md` returns ≥ 1.
      - `grep -c "/forgebook reload" docs/RELEASE-SMOKE.md` returns ≥ 1.
      - `grep -c "/forgebook disable" docs/RELEASE-SMOKE.md` returns ≥ 1.
      - `grep -c "/forgebook enable" docs/RELEASE-SMOKE.md` returns ≥ 1.
      - `grep -c "/forgebook stats" docs/RELEASE-SMOKE.md` returns ≥ 1.
    - Secret-leakage guard: `grep -c 'grep "sk-ant-" logs/latest.log' docs/RELEASE-SMOKE.md` returns 1 (the zero-match assertion preserved).
    - Audit log format referenced: `grep -c "forgebook.audit" docs/RELEASE-SMOKE.md` returns ≥ 1.
    - Pass/Fail criteria section: `grep -c "^## Pass / Fail Criteria" docs/RELEASE-SMOKE.md` returns 1.
    - RC-tagging guidance on failure: `grep -c "v1.0.0-rc" docs/RELEASE-SMOKE.md` returns ≥ 1.
    - Teardown step reminds operator to delete API key: `grep -c "Delete the API key" docs/RELEASE-SMOKE.md` returns ≥ 1.
    - i18n key references from Plan 05-04 are present (demonstrates protocol is current):
      - `grep -c "forgebook.command.reload.success" docs/RELEASE-SMOKE.md` returns ≥ 1.
      - `grep -c "forgebook.command.denied.rate_limited" docs/RELEASE-SMOKE.md` returns ≥ 1.
  </acceptance_criteria>
  <done>
    `docs/RELEASE-SMOKE.md` exists with 9 steps, automated-vs-human tagging, Pass/Fail criteria, and full coverage of all 7 `/forgebook` subcommands. Jar filename correctly uses `1.0.0` (post-Plan 05-01). The `chmod 600` file-permission guard is reinforced. Teardown explicitly reminds the operator to delete the API key.
  </done>
</task>

<task type="auto">
  <name>Task 2: Automated portion of Step 1 — clean build + jar integrity</name>
  <files></files>
  <read_first>
    - docs/RELEASE-SMOKE.md Step 1 (created in Task 1 of this plan)
    - build.gradle (confirms `jarJar` dependency chain)
  </read_first>
  <action>
    This task runs the automated portion of the RELEASE-SMOKE protocol (Step 1 only) as a Phase-5-closing build-health check. The human smoke (Steps 2-9) is deferred to Task 3's checkpoint.

    Commands (run in repo root):
    ```bash
    ./gradlew clean build --no-daemon
    ls -la build/libs/forgebook-1.0.0.jar
    jar tf build/libs/forgebook-1.0.0.jar | grep -E "META-INF/jarjar/.*jsoup.*\\.jar"
    ```

    **Expected results:**
    - `./gradlew clean build` exits 0.
    - `build/libs/forgebook-1.0.0.jar` exists and is >50 KB (typical size ~200-400 KB after jsoup bundle).
    - `jar tf | grep` returns ≥ 1 line matching `META-INF/jarjar/jsoup-relocated-<version>.jar` — confirms the relocated jsoup is nested (jarJar intact).

    **If any of these fails:** STOP. Do NOT proceed to Task 3's checkpoint. Flag for checker review. Likely causes:
    - `relocateJsoup` task broke (check `build/relocated/` for output).
    - `jarJar` dependency chain broke (check `build.gradle` L64-67 — `jarJar files(tasks.relocateJsoup)` + `tasks.named('jarJar') { dependsOn 'relocateJsoup' }`).
    - Plan 05-01's version bump didn't land (jar is named `forgebook-0.1.0.jar`).

    **Do NOT:**
    - Skip the `clean` — stale `build/libs/` contents mask real build-pipeline regressions.
    - Manually chmod or curl anything — this is the automated portion only.
    - Test against a live Anthropic API endpoint — that's Step 5, human-only.
    - Commit the build/ output — it's git-ignored (Phase 1 CFG-06).
  </action>
  <verify>
    <automated>./gradlew clean build --no-daemon &amp;&amp; test -f build/libs/forgebook-1.0.0.jar &amp;&amp; test $(stat -c%s build/libs/forgebook-1.0.0.jar 2>/dev/null || wc -c &lt; build/libs/forgebook-1.0.0.jar) -gt 50000 &amp;&amp; jar tf build/libs/forgebook-1.0.0.jar | grep -qE "META-INF/jarjar/.*jsoup.*\\.jar"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew clean build --no-daemon` exits 0.
    - `build/libs/forgebook-1.0.0.jar` exists.
    - Jar size > 50 KB (sanity check: too-small jar indicates jsoup didn't bundle).
    - `jar tf build/libs/forgebook-1.0.0.jar` output includes at least one line matching `META-INF/jarjar/.*jsoup.*\.jar`.
    - `jar tf` output does NOT include `org/jsoup/` at the jar root (if it does, relocation regressed — jsoup should be at `com/forgebook/shadow/jsoup/` inside the nested jar).
    - No `build/libs/forgebook-0.1.0.jar` exists (proves Plan 05-01 version bump landed; proves `clean` wiped stale artifacts).
  </acceptance_criteria>
  <done>
    `./gradlew clean build` produces `build/libs/forgebook-1.0.0.jar` with the relocated jsoup properly nested via jarJar. This is the Step 1 automation from docs/RELEASE-SMOKE.md — the remaining 8 steps are deferred to the human-verify checkpoint (Task 3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human operator runs Steps 2-9 of docs/RELEASE-SMOKE.md before tagging v1.0.0</name>
  <files>docs/RELEASE-SMOKE.md</files>
  <action>See &lt;how-to-verify&gt; below for the step-by-step operator runbook (Steps 2-9 of RELEASE-SMOKE.md). This is a human-verify checkpoint — Claude cannot run a disposable Minecraft client + dedicated Forge server + real Anthropic API key smoke in a non-GUI harness. Under --auto chain the checkpoint auto-approves (physical smoke deferred to a pre-tag operator session).</action>
  <verify><manual>Operator reports approved (Step 1 green, Steps 2-9 deferred to pre-tag session) or verified (all 9 steps ran green, ready to tag v1.0.0).</manual></verify>
  <done>Operator has read docs/RELEASE-SMOKE.md and either (a) completed the full 9-step walkthrough or (b) explicitly acknowledged deferral to the pre-tag human session. Step 1 (automated build + jar integrity) is green from Task 2. Under --auto chain this resolves automatically.</done>
  <what-built>
    Task 1 created `docs/RELEASE-SMOKE.md` with 9 steps. Task 2 ran Step 1 (clean build + jar integrity) successfully, producing `build/libs/forgebook-1.0.0.jar` with nested jsoup. Steps 2-9 remain — they exercise the packaged jar on a real dedicated server with a real API key, which Claude cannot do in a non-GUI harness.
  </what-built>
  <how-to-verify>
    This checkpoint formalizes REL-05 SC-1 ("Built jar smoke-tested on a clean Forge 47.4.18 dedicated server before first tagged release").

    **Minimum acceptance (for phase close under `--auto` chain):**
    - Task 2's automated Step 1 has passed (Claude did this — jar exists + jsoup relocated + size > 50 KB).
    - The human operator has read `docs/RELEASE-SMOKE.md` and acknowledges that Steps 2-9 must be walked before applying a `v1.0.0` git tag.

    **Full verification (recommended before `git tag v1.0.0`):**
    1. Walk Steps 2-9 of `docs/RELEASE-SMOKE.md` on disposable server + client installations.
    2. Record observed behaviour against each "Expected" line.
    3. If all expectations met: proceed to tag `v1.0.0`.
    4. If any "Expected" diverges: tag as `v1.0.0-rcN` instead (where N = next attempt), file a GitHub issue capturing the regression, fix, repeat from Step 1.

    **Time estimate:** 30-60 min for a smooth run; 2-4 hours if regressions surface and need debugging.

    **Pre-requisites:**
    - Disposable Minecraft 1.20.1 launcher installation (don't use your main).
    - Disposable Forge 1.20.1-47.4.18 dedicated server in a fresh folder.
    - Anthropic API key (single smoke costs ~$0.01 at Haiku pricing).

    **Under `--auto` chain:** This checkpoint auto-approves. REL-05 SC is satisfied by Step 1 being green (packaged jar + relocation verified) + the protocol being documented for the release operator to walk pre-tag. Physical smoke is parked for a dedicated human session before `v1.0.0`.

    **Pass criterion (under auto):** Task 2 passed + `docs/RELEASE-SMOKE.md` exists. Approve to close phase.

    **Pass criterion (full verification):** Human operator reports "all 9 steps passed" after running the protocol.
  </how-to-verify>
  <resume-signal>Type "approved" to accept current state (Step 1 green, Steps 2-9 deferred to pre-tag human session), or "verified" after running the full protocol on a disposable server.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `build/libs/forgebook-1.0.0.jar` → third-party server operator | A jar that works in dev but fails in a packaged run (jsoup relocation silently missing, SimpleChannel miswired) would be a supply-chain-grade bug for the operator. Step 1 catches this. |
| Anthropic API key → log files | `ai_api_key` is in plaintext in `forgebook-server.toml`. If the `ApiKeyScrubFilter` regressed, Step 3's grep would detect `sk-ant-` substrings leaking to `logs/latest.log` — a real privacy and billing incident. |
| Packaged jar → client-classloader firewall | Step 2 explicitly greps for `net.minecraft.client.*` in dedicated server logs. A regression would be a mod-loader crash on server-only installs — a severe deployment failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Information Disclosure | API-key leak in logs | mitigate | Step 3 explicitly greps `logs/latest.log` for `sk-ant-` — a positive match fails the release. ApiKeyScrubFilter (Phase 1 CFG-05) is re-validated on every release. |
| T-05-06-02 | Tampering | jsoup relocation regressed | mitigate | Step 1's `jar tf` grep confirms the relocated nested jar is present. Task 2 runs this automatically in every phase close. |
| T-05-06-03 | Denial of Service | Client-classloader leak crashes dedicated server | mitigate | Step 2 console/log check explicitly says "No client-classloader leakage — nothing about net.minecraft.client.*". A regression would manifest as a NoClassDefFoundError at server startup; human operator catches it. |
| T-05-06-04 | Elevation of Privilege | `/forgebook item` works for non-OP without op_only relaxation | mitigate | Step 7 (disable/enable) + Step 8 (rate-limit from non-OP account) exercise the OP gate + rate limit pair. A regression in Authorizer (Plan 05-04) would surface here. |
| T-05-06-05 | Repudiation | Audit log missing or malformed | mitigate | Step 5 explicitly greps `logs/latest.log` for the `[forgebook.audit]` line with uuid/kind/tokens/latency/outcome fields. Regressions in RequestAuditLogger (Phase 3 SAFE-04) are caught. |
| T-05-06-06 | Information Disclosure | API key left in disposable config post-smoke | mitigate | Step 9 (Teardown) explicitly says "Delete the API key from config/forgebook-server.toml before archiving". A reminder — the operator's discipline is the control, but the protocol flags it. |
| T-05-06-07 | Spoofing | Operator runs smoke against a production API key and accidentally burns production budget | accept | The protocol says "test key or main key — rate limits apply". A single smoke is ≤ $0.01 at Haiku pricing. The operator decides whether to use a test key or accept the main-key cost. |
| T-05-06-08 | Tampering | Smoke protocol relies on i18n key strings (post-Plan 05-04) — if they regress in a future phase, smoke assertions fail silently | mitigate | Step 7 and Step 8 explicitly name the translation keys (`forgebook.command.denied.disabled`, `forgebook.command.denied.rate_limited`, etc.). A key rename would be caught by Plan 05-04's acceptance criteria; the smoke doc is an integration canary. |

</threat_model>

<verification>

1. **Task 1 — file exists:** `test -f docs/RELEASE-SMOKE.md`.
2. **Task 1 — all 9 steps:** `grep -cE "^## Step [1-9] " docs/RELEASE-SMOKE.md` returns 9.
3. **Task 1 — Pass/Fail section:** `grep -c "^## Pass / Fail Criteria" docs/RELEASE-SMOKE.md` returns 1.
4. **Task 2 — build green:** `./gradlew clean build --no-daemon` exits 0; `build/libs/forgebook-1.0.0.jar` exists; `jar tf` shows nested jsoup.
5. **Task 3 — checkpoint posted:** Under `--auto` chain the checkpoint auto-approves; under manual execution the user responds "approved" or "verified".

</verification>

<success_criteria>

- [ ] `docs/RELEASE-SMOKE.md` exists with 9 steps + Pass/Fail + automated/human tagging.
- [ ] Jar filename `forgebook-1.0.0.jar` used consistently (matches Plan 05-01's bump).
- [ ] `chmod 600` cross-reference present (reinforces README security posture).
- [ ] All 7 `/forgebook` subcommands exercised across Steps 5-8.
- [ ] Step 3 secret-leakage grep preserved.
- [ ] `./gradlew clean build --no-daemon` green in Task 2 (Step 1 automated verification passes).
- [ ] Human-verify checkpoint posted; under `--auto` chain it auto-approves (deferred to pre-tag operator).

</success_criteria>

<output>
After completion, create `.planning/phases/05-release-polish/05-06-SUMMARY.md` recording: docs/RELEASE-SMOKE.md created, Task 2 build result (PASS / jar filename / size / jsoup relocation verified), and checkpoint resolution.
</output>
