# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle (directory forms for explicit anchoring per plan)
build/
.gradle/
run/

# other
eclipse

# Files from Forge MDK
forge*changelog.txt

# Backup TOMLs
*.toml.bak

# Disallow stray forgebook-server.toml at repo root or run/ (secrets hygiene, CFG-06)
forgebook-server.toml
# But allow fixture/example TOMLs under config/ (Phase-1 reserved fixtures dir)
!config/forgebook-server.toml
